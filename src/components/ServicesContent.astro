---
// Componente ServicesContent convertido a Tailwind
---

<section
  class="w-full mx-auto mt-[9.5rem] px-5 sm:p-0 sm:pl-[calc(5%_+_112px)]"
>
  <main class="w-full mx-auto mt-10">
    <!-- .section-title reemplazado -->
    <h2
      class="text-[var(--blanco-06)] font-semibold text-[0.8rem] mb-4 uppercase tracking-[1px]"
    >
      Mis servicios
    </h2>

    <!-- .services-grid reemplazado -->
    <div
      class="grid grid-cols-[repeat(auto-fill,minmax(250px,1fr))] gap-[30px]"
    >
      <!-- Starlink Service Card (.service-card reemplazado) -->
      <article
        class="bg-gradient-to-b from-[var(--azul-primario-08)] to-[var(--azul-primario-total)] border border-[var(--blanco-08)] rounded-[20px] px-5 py-10 flex flex-col items-center justify-between h-[250px] transition-transform duration-300 hover:-translate-y-[5px]"
      >
        <!-- .service-logo reemplazado -->
        <div
          class="text-[var(--blanco-total)] text-[2rem] font-bold tracking-[5px] uppercase mt-5"
        >
          STARLINK
        </div>

        <!-- .service-btn reemplazado -->
        <a
          href="/starlink"
          class="w-full max-w-[180px] p-[10px] rounded-[10px] bg-[var(--accent-cyan)] text-[var(--azul-profundo)] border-none text-sm font-semibold cursor-pointer transition-colors duration-300 ease-in hover:bg-[var(--cyan-hover)] text-center no-underline block"
        >
          Ingresar
        </a>
      </article>
    </div>
  </main>
</section>

<style>
  /* Clases conservadas porque no aparecían en el HTML proporcionado */
  @media (width >= 940px) {
    .dashboard-content {
      margin: 8rem 0 0 1rem;
      width: 100%;
      padding: 1.25rem 0;
    }
    .devices-main {
      width: 100%;
      margin: 0;
    }
  }
</style>
